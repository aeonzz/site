# Implementation Plans

Generated by the improve skill on 2026-07-07. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Restore a working lint command | P1 | S | - | DONE |
| 002 | Establish a minimal test and verification baseline | P1 | M | 001 | DONE |
| 003 | Fix project-card image sizing and Open Graph metadata asset | P2 | S | - | DONE |
| 004 | Move Projects into a Content Collections MDX wrapper | P2 | M | 002 | DONE |
| 005 | Use stable sitemap lastModified values | P2 | S | - | BLOCKED (target files are untracked; isolated worktree from `905a34a` cannot contain them) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale - finding fixed independently or approach abandoned)

## Dependency notes

- 002 depends on 001 because a useful verification baseline should include lint, and the current lint script is broken.
- 004 depends on 002 because moving Projects into Content Collections changes rendering and content shape; add at least smoke coverage first so the refactor has a regression signal.
- 003 is independent and can be executed any time.
- 005 is independent and can be executed any time; it only touches sitemap/content metadata and its focused regression test.

## Reconciliation log

- 2026-07-07 (`905a34a`, dirty working tree): verified DONE plans 001-004 against the current working tree. `pnpm lint`, `pnpm exec tsc --noEmit --incremental false`, `pnpm test`, and `pnpm build` all exited 0; build reported 2 Content Collections and 3 documents.
- As of 2026-07-07, no TODO, IN PROGRESS, or BLOCKED plans remained from plans 001-004.
- 2026-07-08 (`905a34a`, dirty working tree): quick audit added plan 005 for stable sitemap `lastModified` values. `pnpm lint`, `pnpm exec tsc --noEmit --incremental false`, `pnpm test`, and `pnpm build` exited 0 during recon; `pnpm audit --json` reported 2 moderate advisories and 0 high/critical advisories.
- 2026-07-08 (`905a34a`, dirty working tree): execute preflight for plan 005 stopped before dispatch. `git status --short -- app/sitemap.ts .content-collections.ts content/projects tests plans/README.md plans/005-stabilize-sitemap-lastmodified.md` showed all target implementation paths and plans as untracked, and `git ls-files -- app/sitemap.ts .content-collections.ts content/projects tests plans/README.md plans/005-stabilize-sitemap-lastmodified.md` returned no tracked files, so an isolated executor worktree from the planned commit would not contain the code the plan targets.

## Findings considered and rejected

- Hydration warning containing `data-scribe-recorder-ready`: rejected as likely caused by a browser extension mutating `<html>`, not repository code.
- Committed secret exposure: rejected; a narrow high-signal credential scan found no obvious committed credentials.
- High or critical dependency security issue: rejected; `pnpm audit --json` reported 2 moderate advisories and 0 high/critical advisories.
- Full dependency remediation plan: deferred; the moderate advisories are transitive through `next` and `@content-collections/mdx`, so monitor/upstream update is more appropriate than urgent local surgery.
- JSON-LD `dangerouslySetInnerHTML`: rejected; the injected values come from local schema objects serialized through `JSON.stringify` with `<` escaped by `serializeJsonLd`, not from raw user-controlled HTML.
- Custom MDX heading ID suffix rendering: rejected for this quick pass; the helper supports `[#id]` suffixes, but current MDX content does not use them, so there is no live user-facing bug to plan.
- Missing root README/product docs: noted but not planned in quick mode; it is DX/docs debt rather than a correctness, security, or test finding.
